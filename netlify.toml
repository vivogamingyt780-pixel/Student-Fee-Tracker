[build]
  command = "pnpm install && pnpm --filter @workspace/fee-tracker run build"
  publish = "artifacts/fee-tracker/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
