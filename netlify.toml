[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/fee-tracker run build"
  publish = "artifacts/fee-tracker/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
